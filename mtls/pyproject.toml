[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Justin N. Pitera", email = "justinpitera@gmail.com" }
]
dependencies = [
    "starlette>=0.45.3",
    "uvicorn>=0.34.0",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/test"]
