export const DEBUG_MODE = false; //typeof window !== 'undefined' ? location.hostname === 'localhost' : false; // Force popup to auto scan an example QR without a camera and prevent showing errors due to missing camera and auto navigate back to homepage from the cancel report

export const defaultLang = 'en';

export const BACKEND_URL = 'https://vikunja.justinpitera.com:7878'; // empty in production - WITHOUT SLASH AT THE END

export const maxFiles = 4;