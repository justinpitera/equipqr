export const DEBUG_MODE = true; // Force popup to auto scan an example QR without a camera and prevent showing errors due to missing camera and auto navigate back to homepage from the cancel report

export const BACKEND_URL = 'https://preview.pitera.co:7878'; // empty in production

export const maxFiles = 4; // TODO migrate this over